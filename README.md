# TRMNL_manual_dashboard

Manual dashboard image generation for a TRMNL display.

This specific version displays current and forecasted weather (hourly for the upcoming 12 hours and summary for three days) using open-meteo, a calendar, moon phase, and indoor temperature from an AirGradient device.

The only dependancy is a font file (currently uses Roboto, downloadable from Google Fonts).
